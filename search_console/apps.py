from django.apps import AppConfig


class SearchConsoleConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'search_console'
    verbose_name = 'Search Console / Sitemap Indexleme'
